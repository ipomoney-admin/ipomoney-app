name: IPO Data Sync

on:
  schedule:
    - cron: '*/15 * * * *' # Har 15 minute mein khud chalega [cite: 2026-02-02]
  workflow_dispatch: # Manual button se chalane ke liye [cite: 2026-02-02]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3 [cite: 2026-02-02]

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' [cite: 2026-02-02]

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 supabase [cite: 2026-02-02]

      - name: Run Scraper
        env:
          SUPABASE_URL: "https://blwjuzmvrvtfklthvfoz.supabase.co" [cite: 2026-02-02]
          SUPABASE_KEY: "sb_secret_D3eHTtrO8atdHTHIReXxXQ_gc9AnEil" # Teri Secret Key [cite: 2026-02-02]
        run: python scraper_final.py [cite: 2026-02-02]
