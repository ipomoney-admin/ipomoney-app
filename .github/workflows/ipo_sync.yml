name: IPO Data Sync

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 supabase

      - name: Run Scraper
        env:
          SUPABASE_URL: "https://blwjuzmvrvtfklthvfoz.supabase.co"
          SUPABASE_KEY: "sb_secret_D3eHTtrO8atdHTHIReXxXQ_gc9AnEil"
        run: python scraper_final.py
